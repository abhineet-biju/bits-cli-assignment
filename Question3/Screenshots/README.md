# Screenshots For Question 3

Add screenshots from the Coursera Linux Lab terminal here.

Required screenshots:

- Creating `original.txt`, `hardlink.txt`, and `symlink.txt`
- `ls -li`
- `stat` output for each file
- Deleting `original.txt`
- `cat hardlink.txt` and `cat symlink.txt` after deletion

